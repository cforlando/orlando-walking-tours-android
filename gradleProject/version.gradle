ext {
    // Remember to update the version in /.travis.yml as well
    androidBuildToolsVersion = '25.0.1'

    versionCode = 2
    semanticVersion = '1.1.0'

    minAndroidVersion = 19
    targetAndroidVersion = 24
}
